## Packages
framer-motion | Complex animations for alarm overlay and transitions
date-fns | Time formatting and date manipulation
react-hook-form | Form handling for alarms and settings
@hookform/resolvers | Zod resolvers for forms

## Notes
- App requires 'Orbitron' font for the digital clock aesthetic
- Audio playback needs user interaction first (browser policy); alarm sound might need a "Start" button interaction on first load if autoplay is blocked
- RTL support needed for Arabic language
